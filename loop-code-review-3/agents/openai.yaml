interface:
  display_name: "Loop Code Review 3"
  short_description: "Choose review severity before each loop"
  default_prompt: "Use $loop-code-review-3 to ask which severity levels to review, then review and fix only those levels."
